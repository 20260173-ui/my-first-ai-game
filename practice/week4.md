# 4주차 실습


##이번주 목표:pygame 충돌 시스템 구현

##AI에게 물어본 질문

**Q1:pygame으로 800x600 창을 하나 만들어줘회색 사각형 오브젝트 2개를 그려줘. 하나는 방향키로 이동 가능, 하나는 화면 중앙 고정해줘. 시작위치는 서로 다르게해줘. 두 오브젝트의 AABB (빨간 사각형)를 함께 표시해줘.**

-AI답변:
항목내용창 크기800 × 600. OBJ 1 (노란 레이블)80×80 · 시작 위치 (100, 100) · 방향키로 이동. OBJ 2 (흰 레이블)100×120 · 화면 정중앙 고정. AABB (빨간 테두리)두 오브젝트를 완전히 감싸는 최소 외접 사각형 (rect.union())충돌 감지.두 오브젝트가 겹치면 화면 상단에 COLLISION! 표시
- 적용결과:실패
- 문제점:사각형에 각각 AABB충돌 시스템의 테두리가 씌워지는것이 아닌 사각형 두개가 서로 AABB 박스의양 끝부분을 담당하고 있어서 서로의 충돌 상황을 알수없는 상태가 만들어짐.

**Q2:내가 생각한것과 다른 느낌으로 나왔네.  각 회색 사각형 마다 aabb 충돌 감지 시스템이 따로따로 존재하게 만들어줘**

-AI답변:핵심 변경 사항입니다.
이전 — 두 오브젝트를 하나로 합친 union AABB 1개
이후 — 각 오브젝트가 자신만의 독립 AABB를 소유
OBJ 1OBJ 2AABB 색빨강주황위치 갱신매 프레임 (이동 따라감)고정 (1회 생성)padding14px14px
충돌 판정은 두 단계로 표시됩니다.

AABB ↔ AABB — 두 AABB 박스가 겹치면 HIT (먼저 감지)
MESH ↔ MESH — 실제 회색 사각형이 겹쳐야 HIT

AABB가 먼저 반응하고, 그보다 더 가까워졌을 때 MESH가 반응하는 순서를 확인할 수 있습니다.
- 적용결과:성공. 그러나 생각과는 다른 결과가 역시나 나옴. 두 스프라이트의 주변을 감싸는 AABB 박스가 회색 사각형의 테두리 바깥쪽을 감싸고 실제 충돌은 MESH라는 배우지 않은 방식을 사용함

**Q3:2단계로 나눠서 감지하지말고 aabb박스가 회색 사각형에 딱 붙게 만드는 방법 알려줘. MESH는 사용하지 말아줘**

-AI답변:PADDING을 0으로 없애야함. PADDING을 완전히 제거하고 aabb = obj.copy()로 처리. 빨간 테두리가 회색 사각형 테두리에 정확히 겹치고, 두 AABB가 닿는 순간 바로 HIT로 표시.
- 적용결과:성공

**Q4:이제 이  base 64 코드를 이용해볼꺼야. 이 코드에서 _ADVENTURER 와 _STONE  오브젝트를 사용할꺼야. 스톤은 고정되어있고 어드벤처는 위,아래,왼쪽,오른쪽 키에 따라 5픽셀씩 움직이게 해줘. 두 오브젝트 위에 AABB 식으로 충돌감지 시스템을 만들어서 박스가 표시되게 해줘. 스톤과 어드벤처가 만나면 화면이 노란색으로 바뀌게 만들어줘**

-AI답변:응답 중지됨
- AI가 base64 코드를 읽는데 너무 많은 시간이 소모되어 방법을 바꿔야했음.

**Q5:오브젝트를 내가 직접 넣을게 어디에 넣어야할지 알려주고 내가 넣을부분만 코드 비워서 만들어줘**

AI답변:스프라이트를 넣어야 할 위치에 주석을 이용하여 표시
- 적용결과:성공

**Q6:이제 고정되어있는 stone 오브젝트가 천천히 회전하게 해줘 그리고  Z 키를 누르면 회전 속도가 빨라지게 해줘. 회전하는 오브젝트의 OBB 를 초록색 사각형으로 시각적으로 보이게 만들어줘**

-AI답변:회전하는 스프라이트에 OBB를 그리려면 pygame.transform.rotate와 함께 4개의 꼭짓점을 직접 계산해야 합니다.파일 조회함, 명령 실행함, 파일 읽음파일 조회함, 명령 실행함, 파일 읽음추가된 핵심 내용입니다.
get_obb_corners() — stone의 중심, 원본 크기, 현재 각도를 받아 2D 회전 공식(x' = ox·cos − oy·sin)으로 4개의 꼭짓점 좌표를 계산합니다. 회전된 이미지 크기가 아닌 원본 크기를 기준으로 하기 때문에 박스가 스프라이트에 정확히 붙습니다.
aabb_hit_obb() (SAT) — AABB와 OBB의 충돌을 분리축 정리(Separating Axis Theorem)로 검사합니다. 총 4개의 축(AABB 수평/수직, OBB의 두 로컬 축)에 두 도형을 투영해서 하나라도 겹치지 않으면 충돌 없음으로 판정합니다.
회전 — 기본 0.5도/프레임, Z키를 누르면 3.0도/프레임으로 빨라집니다.
- 적용결과:성공

**Q7:현재 코드를 수정해서 세 가지 충돌 방식을 동시에 표시해줘.
원형,aabb,obb형식 각 스프라이트에 모두 만들어줘 
또한 이런 시스템도 구현해줘
- 원형: 파란 원, 충돌 시 상단에 “Circle: HIT” 표시
- AABB: 빨간 사각형, 충돌 시 “AABB: HIT” 표시
- OBB: 초록 사각형, 충돌 시 “OBB: HIT” 표시
- 세 방식의 충돌 결과를 화면 왼쪽 상단에 동시에 표시해줘.

-AI답변:각 스프라이트마다 세 가지 BB가 모두 그려집니다.
방식색상Adventurer 기준Stone 기준Circle파란 원adv_rect 너비 절반 반지름원본 이미지 너비 절반 반지름AABB빨간 사각형adv_rect 그대로회전된 이미지의 bounding rectOBB초록 사각형회전 없으므로 AABB와 동일원본 크기 + 회전각으로 꼭짓점 계산 (SAT)
Stone이 회전하면서 세 방식의 감지 타이밍이 달라지는 걸 볼 수 있습니다. 모서리 부분에서 Circle은 늦게 반응하고, AABB는 빠르게 반응하며, OBB가 가장 정확하게 반응합니다.
- 적용결과:성공
- 특이사항:AABB는 고정되어있기에 스프라이트가 회전하자 스프라이트가 있지 않은 위치에서도 충돌이라고 감지하였다.

##시행착오 및 해결과정
- 처음 AABB방식을 적용할때 AABB 박스가 각 회색 사각형의 모서리에 생성되는것이 아닌 그 사각형 둘이 AABB 박스의 모서리를 만들어버리는 기이한 현상이 발생함. AI에게 다시 한번 각 사각형 위에 AABB박스를 그려달라고 요청을 함. 새로운 문제로 배우지 않았고 요청하지도 않은 MESH 가 나옴. 다시 제거해달라고 요청을 함. 명령을 다시 수행하여 문제 해결
- 클로드 AI를 사용하다가 제미나이로 다시 이동하였을때 제미나이에게 기존에 주어진 정보가 없고 완전히 모르는 상태에서 명령을 내리다보니 오류가 발생함-결국 다시 클로드 AI 사용을 위해 시간을 기다려서 작업을 완료함. 또한 제미나이에게 사전 작업한 작업물들의 정보와 현재 작업중인 프로그램에 관한 정보를 주니 다시 잘 작동함.

##(다시)배운점
- 실습을 하며 다시한번 AI에게 명령을 내릴때는 세세하고 정확하게 자신이 원하는것을 구체적으로 요청해야 원하는 결과가 나오고 오류가 발생하지 않는다는 사실을 느낌. 세세하게 명령을 내리지 않으니 시행착오 첫번째 문장처럼 전혀 생각치도 못한 결과물이 등장함.
- AI에게 기존 다른 AI가 하던 작업을 새 AI에게 요청할때는 사전 준비와 정보를 충분히 제공하여야 자신이 원하는 결과물을 얻을수 있다는 정보를 새롭게 얻음. 앞으로 AI를 갈아타며 작업을 진행할때는 새로운 AI에게 기존 작업에 관한 정보를 충분히 제공하겠다는 다짐또한 할수 있게됨.


## 오늘 한 것

- 원형 / AABB / OBB Bounding Box 시각화
- 세 방식의 충돌 판정 차이 관찰

## 관찰 내용

### 원형 vs AABB

사각형 오브젝트에 크기에 딱 맞는 AABB를 적용하니, 모서리와 꼭짓점 부분을 포함해 스프라이트가 충돌했을 때 모두 충돌 판정이 나는 것을 확인할 수 있었다.

원형 방식을 적용했을 때는 스프라이트가 원형 충돌 감지 범위 밖에서 서로 겹침에도 불구하고, 원형 충돌 감지 시스템은 충돌하지 않았다고 판단하는 결과가 발생했다.

### AABB vs OBB

AABB 방식은 스프라이트가 회전하면 스프라이트의 대각선이나 가로세로 크기에 맞춰 크기가 커졌다 작아졌다 하며 빈 공간이 생기거나 충돌 부위가 달라지는등 모습을 보였다.

OBB 방식은 스프라이트가 회전함에 따라 충돌 감지 영역도 함께 회전하는 모습을 보였다.

## AI와의 대화에서 배운 것

AABB 방식을 그리는 코드에 대한 설명을 요청했을 때, 아래 코드에서 `pygame.draw.rect`의 마지막 인자 `2`가 핵심이라는 답변이 흥미로웠다.

```python
c1 = HIT_COLOR if hit else RED1
c2 = HIT_COLOR if hit else RED1
pygame.draw.rect(screen, c1, aabb1, 2)
pygame.draw.rect(screen, c2, aabb2, 2)
```

마지막 인자가 `0`이면 사각형 내부를 색으로 채우고, `1` 이상이면 그 픽셀 수만큼의 테두리선만 그린다는 것을 새롭게 알게 되었다. 이 인자를 활용해 pygame에서 Bounding Box를 시각화한다는 것을 이해할 수 있었다.

## 내 게임에 적용한다면

만들고 싶은 게임은 **탄막 슈팅 게임**으로, 우주선에 탄막이 피격되는 것을 구현하기 위해 다음과 같이 적용할 계획이다.

- **우주선 → AABB 방식**
  탄막 슈팅 게임에서 우주선이 회전하는 경우는 거의 없으며, 피격에 빠르게 반응해야 하므로 AABB가 적합하다. 원형 방식을 적용하면 탄막이 우주선 스프라이트에 맞았음에도 피격 판정이 나타나지 않거나, 원형 박스가 우주선보다 크면 분명히 피했는데도 맞았다고 판단하는 문제가 생길 수 있다.

- **총탄 → 원형 방식**
  탄환은 회전할 일이 없으며, 둥근 탄환 모양에는 원형 방식이 더 자연스럽게 맞는다. 또한 탄막 슈팅 장르 특성상 많은 양의 탄막이 쏟아지는데, 이를 모두 계산하기 위해서는 OBB나 AABB보다 연산이 가벼운 원형 방식이 적합하다.
